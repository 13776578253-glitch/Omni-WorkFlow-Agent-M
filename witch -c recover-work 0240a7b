[33mcommit 0240a7b528f6357a6791f126d3e2c1e7674aee19[m
Author: cpp <13776578253@163.com>
Date:   Wed Mar 11 00:34:12 2026 +0800

    cpp
    
    不完善的ui逻辑，存档点，初步完成
    
    When in recording mode, automatically lock the top area into its compact state based on content scroll. Added isAutoCompactLocked state and handleContentScroll in workflow.tsx; pass onScrollOffsetChange to WorkflowContentArea and forcedCompact to WorkflowTopArea. Workflow_ContentArea now reports vertical scroll offset (onScroll + scrollEventThrottle). Workflow_Top_Area respects forcedCompact by animating to compact and short-circuiting expansion logic in gesture release/terminate handlers, and reports height changes as before. This prevents the top panel from expanding while the user has scrolled past the threshold during recording.

[1mdiff --git a/front_end/omni-workflow-agent-m/app/(main)/workflow.tsx b/front_end/omni-workflow-agent-m/app/(main)/workflow.tsx[m
[1mindex c0608c0..600678e 100644[m
[1m--- a/front_end/omni-workflow-agent-m/app/(main)/workflow.tsx[m
[1m+++ b/front_end/omni-workflow-agent-m/app/(main)/workflow.tsx[m
[36m@@ -80,6 +80,7 @@[m [mexport default function WorkflowScreen({ setPagerScrollEnabled }: WorkflowScreen[m
   const [keyboardHeight, setKeyboardHeight] = useState(0);[m
   const [mode, setMode] = useState<WorkflowMode>('welcome');[m
   const [topAreaHeight, setTopAreaHeight] = useState(TOP_AREA_EXPANDED_HEIGHT);[m
[32m+[m[32m  const [isAutoCompactLocked, setIsAutoCompactLocked] = useState(false);[m
   // 消息列表 / 测试[m
   const [messages, setMessages] = useState<WorkflowMessage[]>([]);[m
   const [quickActionNames, setQuickActionNames] = useState<QuickActionNames>(DEFAULT_QUICK_ACTION_NAMES);[m
[36m@@ -178,6 +179,17 @@[m [mexport default function WorkflowScreen({ setPagerScrollEnabled }: WorkflowScreen[m
     return margin;[m
   }, [insets.bottom, keyboardHeight]);[m
 [m
[32m+[m[32m  const handleContentScroll = useCallback((offsetY: number) => {[m
[32m+[m[32m    if (mode !== 'recording') return;[m
[32m+[m[32m    if (offsetY > 100) {[m
[32m+[m[32m      if (!isAutoCompactLocked) setIsAutoCompactLocked(true);[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m[32m    if (isAutoCompactLocked && offsetY <= 100) {[m
[32m+[m[32m      setIsAutoCompactLocked(false);[m
[32m+[m[32m    }[m
[32m+[m[32m  }, [isAutoCompactLocked, mode]);[m
[32m+[m
   // 录音 波形原点 计算 / 测试 / 待拆分[m
   const recordingDots = useMemo([m
     () =>[m
[36m@@ -299,11 +311,13 @@[m [mexport default function WorkflowScreen({ setPagerScrollEnabled }: WorkflowScreen[m
           mode={mode}[m
           messages={messages}[m
           contentPaddingTop={mode === 'recording' ? topAreaHeight + TOP_AREA_OFFSET : undefined}[m
[32m+[m[32m          onScrollOffsetChange={handleContentScroll}[m
         />[m
         <View style={styles.topAreaDock}>[m
           <WorkflowTopArea[m
             mode={mode}[m
             onHeightChange={mode === 'recording' ? setTopAreaHeight : undefined}[m
[32m+[m[32m            forcedCompact={mode === 'recording' && isAutoCompactLocked}[m
           />[m
         </View>[m
 [m
[1mdiff --git a/front_end/omni-workflow-agent-m/components/workflow/Workflow_ContentArea.tsx b/front_end/omni-workflow-agent-m/components/workflow/Workflow_ContentArea.tsx[m
[1mindex cb4b5b2..5fa0838 100644[m
[1m--- a/front_end/omni-workflow-agent-m/components/workflow/Workflow_ContentArea.tsx[m
[1m+++ b/front_end/omni-workflow-agent-m/components/workflow/Workflow_ContentArea.tsx[m
[36m@@ -1,5 +1,5 @@[m
 ﻿import React from 'react';[m
[31m-import { FlatList, StyleSheet, Text, View } from 'react-native';[m
[32m+[m[32mimport { FlatList, StyleSheet, Text, View, type NativeScrollEvent, type NativeSyntheticEvent } from 'react-native';[m
 [m
 import { WorkflowWelcomeArea } from '@/components/workflow/workflow_Welcome_Area';[m
 import type { WorkflowMode } from '@/constants/workflow_type';[m
[36m@@ -33,12 +33,14 @@[m [minterface WorkflowContentAreaProps {[m
   mode: WorkflowMode;[m
   messages?: WorkflowMessage[];[m
   contentPaddingTop?: number;[m
[32m+[m[32m  onScrollOffsetChange?: (offsetY: number) => void;[m
 }[m
 [m
 export function WorkflowContentArea({[m
   mode,[m
   messages = DEFAULT_WORKFLOW_MESSAGES,[m
   contentPaddingTop,[m
[32m+[m[32m  onScrollOffsetChange,[m
 }: WorkflowContentAreaProps) {[m
   const cardColor = useThemeColor({}, 'card');[m
   const textColor = useThemeColor({}, 'text');[m
[36m@@ -48,6 +50,10 @@[m [mexport function WorkflowContentArea({[m
     return <WorkflowWelcomeArea bgColor={bgColor} textColor={textColor} />;[m
   }[m
 [m
[32m+[m[32m  const handleScroll = (event: NativeSyntheticEvent<NativeScrollEvent>) => {[m
[32m+[m[32m    onScrollOffsetChange?.(event.nativeEvent.contentOffset.y);[m
[32m+[m[32m  };[m
[32m+[m
   return ([m
     <FlatList[m
       data={messages}[m
[36m@@ -62,6 +68,8 @@[m [mexport function WorkflowContentArea({[m
       showsVerticalScrollIndicator[m
       keyboardShouldPersistTaps="handled"[m
       keyboardDismissMode="on-drag"[m
[32m+[m[32m      onScroll={handleScroll}[m
[32m+[m[32m      scrollEventThrottle={16}[m
       renderItem={({ item }) => {[m
         const isUser = item.role === 'user';[m
         return ([m
[1mdiff --git a/front_end/omni-workflow-agent-m/components/workflow/Workflow_Top_Area.tsx b/front_end/omni-workflow-agent-m/components/workflow/Workflow_Top_Area.tsx[m
[1mindex c228fd3..1ac127e 100644[m
[1m--- a/front_end/omni-workflow-agent-m/components/workflow/Workflow_Top_Area.tsx[m
[1m+++ b/front_end/omni-workflow-agent-m/components/workflow/Workflow_Top_Area.tsx[m
[36m@@ -10,6 +10,7 @@[m [mimport { useThemeColor } from '@/hooks/use-theme-color';[m
 interface WorkflowTopAreaProps {[m
   mode: WorkflowMode;[m
   onHeightChange?: (height: number) => void;[m
[32m+[m[32m  forcedCompact?: boolean;[m
 }[m
 // 常量定义[m
 export const TOP_AREA_EXPANDED_HEIGHT = 220;     // 展开状态高度[m
[36m@@ -31,7 +32,7 @@[m [mfunction formatElapsed(seconds: number): string {[m
   return `${mm}:${ss}`;[m
 }[m
 [m
[31m-export function WorkflowTopArea({ mode, onHeightChange }: WorkflowTopAreaProps) {[m
[32m+[m[32mexport function WorkflowTopArea({ mode, onHeightChange, forcedCompact }: WorkflowTopAreaProps) {[m
   const bgColor = useThemeColor({ light: '#F3F4F6', dark: '#2A2A2E' }, 'background');[m
   const textColor = useThemeColor({}, 'text');[m
   const waveColor = useThemeColor({ light: '#94A3B8', dark: '#8FA0BF' }, 'text');   // 波形图[m
[36m@@ -84,6 +85,12 @@[m [mexport function WorkflowTopArea({ mode, onHeightChange }: WorkflowTopAreaProps)[m
     extrapolate: 'clamp',[m
   });[m
 [m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    if (forcedCompact && !isCompact) {[m
[32m+[m[32m      animateTo(true);[m
[32m+[m[32m    }[m
[32m+[m[32m  }, [animateTo, forcedCompact, isCompact]);[m
[32m+[m
   useEffect(() => {[m
     if (!onHeightChange) return;[m
     const id = panelHeightAnim.addListener(({ value }) => {[m
[36m@@ -130,6 +137,10 @@[m [mexport function WorkflowTopArea({ mode, onHeightChange }: WorkflowTopAreaProps)[m
         // 手势释放[m
         onPanResponderRelease: (_evt, gestureState) => {[m
           if (axisLockRef.current !== 'vertical') return;[m
[32m+[m[32m          if (forcedCompact) {[m
[32m+[m[32m            animateTo(true);[m
[32m+[m[32m            return;[m
[32m+[m[32m          }[m
           panelHeightAnim.stopAnimation((v) => {[m
             const mid = (TOP_AREA_COMPACT_HEIGHT + TOP_AREA_EXPANDED_HEIGHT) / 2;[m
             const byDragDirection = gestureState.dy < -12 ? true : gestureState.dy > 12 ? false : v <= mid;[m
[36m@@ -139,6 +150,10 @@[m [mexport function WorkflowTopArea({ mode, onHeightChange }: WorkflowTopAreaProps)[m
 [m
         // 手势中断[m
         onPanResponderTerminate: () => {[m
[32m+[m[32m          if (forcedCompact) {[m
[32m+[m[32m            animateTo(true);[m
[32m+[m[32m            return;[m
[32m+[m[32m          }[m
           panelHeightAnim.stopAnimation((v) => {[m
             const mid = (TOP_AREA_COMPACT_HEIGHT + TOP_AREA_EXPANDED_HEIGHT) / 2;[m
             animateTo(v <= mid);[m
